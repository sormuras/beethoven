sudo: false
dist: trusty
language: java

notifications:
  email:
    on_success: always
    on_failure: always

install:
# Download pro release and use it...
#- wget --no-check-certificate https://github.com/forax/pro/releases/download/v0.9.167/pro-9-b167-linux.tar.gz
#- tar -xzf pro-9-b167-linux.tar.gz
#- export PRO="$PWD/pro/bin/pro"

# Download jdk-9 and "install" it into "~/jdk-9"
- cd ~
- wget http://download.java.net/java/jdk9/archive/168/binaries/jdk-9-ea+168_linux-x64_bin.tar.gz
- tar -xzf jdk-9-ea+168_linux-x64_bin.tar.gz
- JAVA_HOME=~/jdk-9
- PATH=$JAVA_HOME/bin:$PATH
- java -version

# Download pro source and build it
#- wget --no-check-certificate https://github.com/sormuras/pro/archive/tools.zip
#- unzip tools.zip
#- cd pro-tools
#- chmod u+x build.sh
#- ./build.sh
#- export PRO="$PWD/target/pro/bin/pro"

script:
- cd $TRAVIS_BUILD_DIR
- jshell --show-version bach.jsh
- find target/bach -type d
